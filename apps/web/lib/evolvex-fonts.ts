import { Special_Elite } from "next/font/google";

export const evolvexTypewriter = Special_Elite({
  weight: "400",
  subsets: ["latin"],
  variable: "--font-evolvex-typewriter",
  display: "swap",
});
